# name: test/sql/scanner/http_sqlite_minimal.test
# description: Minimal test to verify HTTP SQLite VFS is registered
# group: [sqlite_scanner]

require sqlite_scanner

# Just verify that the extension loads successfully
# The actual HTTP functionality requires httpfs to be loaded
statement ok
SELECT 'SQLite scanner with HTTP support loaded';